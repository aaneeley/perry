func test(): string {
    println("executing...");
    var i: int = 0;
    while (i < 10) {
        i = i + 1;
        println(i);
        if (i == 5) {
            return "bonk";
        }
    }
    println("over executed. This is  bad!");
}

println(test());
